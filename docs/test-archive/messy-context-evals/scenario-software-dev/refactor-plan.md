# parser 重构计划（0805 立项）

## 背景

旧 parser 三种格式（CSV / 定宽 / EDI）混在一个 3000 行文件里，新人改不动，出过两次线上事故。

## 目标

- 拆为 ParserV2，按格式分模块，错误信息可定位。
- **验收标准：单测覆盖 ≥80%；性能不低于现状的 20% 以内（即最差慢 20% 可接受）；旧 parser 在全部格式迁移完成并验证后删除。**

## 范围

- 仅解析层，不动上传和存储链路。
- 已知的竞态问题不在本次范围（另行安排）。
